name: Installer CI

on:
  push:
    branches: [main]
    paths:
      - 'installer/**'
      - 'module-catalog.json'
      - '.github/workflows/installer-ci.yml'
  pull_request:
    paths:
      - 'installer/**'
      - 'module-catalog.json'
      - '.github/workflows/installer-ci.yml'

jobs:
  test:
    name: Test (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: '1.25'
          cache-dependency-path: installer/go.sum

      - name: Install Fyne build prerequisites (Ubuntu)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1-mesa-dev xorg-dev

      - name: Stub embed files
        shell: bash
        working-directory: installer
        run: |
          touch payload/installer-payload.tar.gz
          touch payload/node-runtime.tar.gz
          touch payload/updater-bin

      - name: go vet
        working-directory: installer
        run: go vet ./...

      - name: go test
        working-directory: installer
        run: go test ./...

      - name: go build (installer)
        working-directory: installer
        run: go build -o /tmp/canvas-toolchain-installer .

      - name: go build (updater stub)
        working-directory: installer
        run: go build -o /tmp/canvas-toolchain-updater ./update/cmd/updater
