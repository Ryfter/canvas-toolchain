{
  "name": "canvas-toolchain",
  "version": "1.0.0",
  "description": "Monorepo for Curriculum Intelligence, Canvas Design Studio, and Command & Control MCP servers",
  "license": "MIT",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/shared-types && npm run build --workspace=packages/shared-llm && npm run build --workspace=packages/module-contract && npm run build --workspace=packages/curriculum-intelligence && npm run build --workspace=packages/module-video && npm run build --workspace=packages/module-oral-assessment && npm run build --workspace=packages/module-group-builder && npm run build --workspace=packages/module-roster && npm run build --workspace=packages/module-peerassessment && npm run build --workspace=packages/canvas-design-studio && npm run build --workspace=packages/command-and-control",
    "test": "npm run test --workspaces --if-present",
    "dev:ci": "npm run dev --workspace=packages/curriculum-intelligence",
    "dev:cds": "npm run dev --workspace=packages/canvas-design-studio",
    "dev:cc": "npm run dev --workspace=packages/command-and-control"
  },
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  }
}
